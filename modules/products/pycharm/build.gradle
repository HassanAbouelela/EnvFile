plugins {
    id "java"
    id "org.jetbrains.intellij"
}

intellij {
    version.set(jetbrains.version)
    plugins.set([jetbrains.pycharm])
}

dependencies {
    implementation project(":envfile-platform")
    implementation "org.jetbrains:annotations:23.0.0"
}
